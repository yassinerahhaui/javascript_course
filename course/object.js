// this is object person

var person = {
    fname : "yassine",
    lname : "rahhaoui",
    age : 28
}

// this function for write data
function print(x) {
    document.write(x +"<br>");
}

print(person.fname);

print(person.fname+" "+person.lname+" "+person.age);